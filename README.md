# Docsy — Next.js App Router Frontend (production-ready)

## Quickstart

1. npm install
2. npm run dev
3. Open http://localhost:3000


This project uses Next.js App Router, TailwindCSS, react-pdf, and includes:
- Section selection (draw boxes)
- Drag / resize handles
- Multi-select (Shift+click)
- Coordinate conversion to PDF points before calling the API
- Mock detection route at /api/detect (app router handler)

Deploy to Vercel: connect repo; framework auto-detected as Next.js.
